import { SearchCountriesPipe } from './search-countries.pipe';

describe('SearchCountriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCountriesPipe();
    expect(pipe).toBeTruthy();
  });
});
